
services:
  db:
    image: postgres:16.3
    container_name: postgres_cont
    environment:
      POSTGRES_DB: social
      POSTGRES_USER: samualhalder
      POSTGRES_PASSWORD: samualpass
    volumes:
      - db-data:/var/lib/postgresql/data
    ports:
      - "5433:5432"

volumes:
  db-data:
